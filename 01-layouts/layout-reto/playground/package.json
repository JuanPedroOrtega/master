{
  "name": "desafio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build:css && live-server --port=8080 --open=src/",
    "build:css": "sass src/styles.scss src/styles.css --no-source-map",
    "watch:css": "sass --watch src/styles.scss src/styles.css --no-source-map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.1.10",
    "live-server": "^1.2.2",
    "sass": "^1.26.5"
  }
}
